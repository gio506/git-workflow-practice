name: Docs CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  structure-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify required files
        shell: bash
        run: |
          required_files=(
            "README.md"
            "CHEATSHEET.md"
            "FILES_EXPLAINED.md"
            ".gitignore"
            "examples/project-log.txt"
            "scripts/validate_repo.sh"
            "docs/commit-style.md"
            "docs/review-checklist.md"
            ".github/PULL_REQUEST_TEMPLATE.md"
            ".github/ISSUE_TEMPLATE/bug.md"
            ".github/ISSUE_TEMPLATE/feature.md"
          )
          for file in "${required_files[@]}"; do
            test -f "$file" || {
              echo "Missing required file: $file"
              exit 1
            }
          done

  markdown-lint:
    runs-on: ubuntu-latest
    needs: structure-check
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lint Markdown files
        uses: DavidAnson/markdownlint-cli2-action@v19
        with:
          globs: |
            **/*.md

  shell-syntax:
    runs-on: ubuntu-latest
    needs: markdown-lint
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate repo helper syntax
        run: bash -n scripts/validate_repo.sh

  practice-flow-check:
    runs-on: ubuntu-latest
    needs: shell-syntax
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Make validation script executable
        run: chmod +x scripts/validate_repo.sh

      - name: Validate repo learning flow
        run: bash scripts/validate_repo.sh

  final-status-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: always()
    needs:
      - structure-check
      - markdown-lint
      - shell-syntax
      - practice-flow-check
    steps:
      - name: Aggregate results
        shell: bash
        env:
          STRUCTURE: ${{ needs.structure-check.result }}
          MARKDOWN: ${{ needs.markdown-lint.result }}
          SYNTAX: ${{ needs.shell-syntax.result }}
          PRACTICE: ${{ needs.practice-flow-check.result }}
        run: |
          for result in "$STRUCTURE" "$MARKDOWN" "$SYNTAX" "$PRACTICE"; do
            if [ "$result" = "failure" ] || [ "$result" = "cancelled" ]; then
              exit 1
            fi
          done
